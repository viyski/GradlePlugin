apply plugin: 'groovy'
apply plugin: 'maven'


dependencies{
    implementation gradleApi()
    implementation localGroovy()
}

repositories{
    mavenCentral()
}

uploadArchives{
    repositories{
        mavenDeployer{
//            repository(url: uri('../release'))
//            pom.groupId = 'com.gralde'
//            pom.artifactId = 'testplugin'
//            pom.version = 1.0
            repository(url: "http://127.0.0.1:8081/repository/GraldePluginTest/"){
                authentication(userName: "admin", password: "admin123")
            }
            pom.project{
                version '1.0'
                groupId "com.gralde"
                artifactId "testplugin"
                description 'gradle plugin test'
            }
        }

    }
}